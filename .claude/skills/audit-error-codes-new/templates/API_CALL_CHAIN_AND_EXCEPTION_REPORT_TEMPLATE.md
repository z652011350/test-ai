# [Kit 名称] API 调用链路和异常分支报告

## 一、API 清单

### 1.1 JS/NAPI 层 Helper 函数（静态方法）

| API 名称 | 说明 |
|---------|------|
| [示例] | [说明] |

### 1.2 实例方法

| API 名称 | 说明 |
|---------|------|
| [示例] | [说明] |

### 1.3 观察者方法

| API 名称 | 说明 |
|---------|------|
| [示例] | [说明] |

### 1.4 NDK 层 API

| API 名称 | 说明 |
|---------|------|
| [示例] | [说明] |

---

## 二、API 调用链路分析

### 2.1 [API 名称] 调用链路

```
JavaScript 层
    ↓ [API 调用]
[napi_xxx.cpp:行号] FunctionName()
    ├─ [line:XX] 检查参数数量 (argc >= N)
    │   └─ ❌ [错误码] - 参数数量不正确
    ├─ [line:XX] 检查参数类型
    │   └─ ❌ [错误码] - 参数类型错误
    ↓
[impl_xxx.cpp:行号] InstanceMethod()
    ├─ [line:XX] 检查对象状态
    │   └─ ❌ [错误码] - 状态不合法
    ├─ [line:XX] 检查对象有效性
    │   └─ ❌ [错误码] - 对象为空
    ↓
[service_xxx.cpp:行号] ServiceMethod()
    ├─ [line:XX] 验证权限
    │   └─ ❌ [错误码] - 权限被拒绝
    ├─ [line:XX] 执行操作
    │   └─ ❌ [错误码] - 操作失败
    └─ ❌ [错误码] - 通用系统错误
```

#### 异常分支汇总

| 层级 | 文件:行号 | 错误码 | 触发条件 |
|------|----------|--------|---------|
| NAPI 参数 | napi_xxx.cpp:123 | 401 | 参数数量不正确 |
| NAPI 对象 | napi_xxx.cpp:156 | 6800301 | 对象为空 |
| Framework | impl_xxx.cpp:234 | 6800103 | 状态不合法 |
| Service | service_xxx.cpp:567 | 6800301 | 操作失败 |

---

### 2.2 [API 名称] 调用链路

[按相同格式列出其他所有 API...]

---

## 三、异常分支统计

### 3.1 按错误码类型统计

| 错误码 | 出现次数 | 说明 |
|---------|----------|------|
| [6800101] | [次数] | 无效参数 |
| [6800301] | [次数] | 系统错误 |

### 3.2 按层级统计

| 层级 | 异常分支数量（估算） |
|------|-------------------|
| NAPI 层 | [N]+ 处 |
| Framework 层 | [N]+ 处 |
| Service 层 | [N]+ 处 |

---

## 四、错误处理机制分析

### 4.1 错误码转换流程

```
Native 层错误码 (负数，如 -62980097)
    ↓
NapiAudioError::ThrowError(int32_t code)
    ↓
std::to_string(code)  // ⚠️ 问题：转字符串
    ↓
napi_throw_error(env, stringCode, message)
    ↓
JavaScript 层接收字符串类型错误码  // ❌ 不符合规范
```

### 4.2 错误码映射表

**文件**: [文件路径]

| Native 错误码 | JS 层错误码 | 映射状态 |
|---------------|-------------|---------|
| ERR_INVALID_PARAM | 6800101 | ✅ 已映射 |
| ERR_NOT_SUPPORTED | 6800104 | ✅ 已映射 |
| ERR_ILLEGAL_STATE | 6800103 | ❌ 未映射 |
| ... | ... | ... |

**映射覆盖率统计**:
- 已映射: [N] 个 ([百分比]%)
- 未映射: [N] 个 ([百分比]%)

### 4.3 异步调用错误处理

- 描述异步错误处理机制
- 错误如何传递到完成回调

---

## 五、典型场景错误码流程

### 5.1 正常流程

```
1. createXXX(options)
   ↓ 返回实例
   ✅ SUCCESS (0)

2. instance.start()
   ↓ 返回 undefined
   ✅ SUCCESS (0)

3. instance.method()
   ↓ 返回结果
   ✅ SUCCESS (0)
```

### 5.2 参数错误流程

```
createXXX()
   ↓ 参数类型不是 object
   ❌ [错误码] (401)
   ↓ JavaScript 层接收
   {
     code: [错误码],
     message: "[错误消息]"
   }
```

### 5.3 状态错误流程

```
instance.method()
   ↓ 对象状态不支持当前操作
   ❌ [错误码] (6800103)
   ↓ JavaScript 层接收
   {
     code: [错误码],
     message: "[错误消息]"
   }
```

---

## 六、总结

### 6.1 核心问题

| 问题类型 | 数量 | 说明 |
|---------|------|------|
| 错误码转字符串 | [N] 处 | 错误码被转换为字符串 |
| 错误码精度降低 | [N] 处 | 三元操作符简化错误判断 |
| 错误码映射不足 | [N] 个 ([百分比]%) | 未正确映射的错误码数量 |

### 6.2 调用链路模式总结

- [模式描述]
- [模式描述]

---

**报告生成时间**: [日期]
**审核项目**: [Kit 名称]
**项目路径**: [路径]
